# Archived Replit files

These are the original Replit configuration files and unused generated artwork, retained as migration history. They are not active application configuration and are not included in the Python wheel. Paths in `.replit` describe the former root-level application layout.

For current startup and hosting, use [development](../../development.md) and [deployment](../../deployment.md).
